import { Search } from 'lucide-react';

export default function SearxLogo() {
  return (
    <div className="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center text-white">
      <Search size={16} strokeWidth={3} />
    </div>
  );
}
